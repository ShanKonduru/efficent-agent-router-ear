# EAR v0.10.1 Release Notes (Draft)

## Summary

v0.10.1 completes the core EAR delivery track for E1-E8 and advances CI/CD hardening with strict coverage enforcement and security-report artifact validation.

## Highlights

- E4 CLI implemented with `route`, `inspect-models`, and `stats` commands.
- E6 Guardrails implemented for prompt-injection and PII detection with vetted-provider filtering.
- E7 Metrics implemented with session-level aggregation and reset behavior.
- E8 MCP server implemented with `route_and_execute` tool and `ear://session/stats` resource.
- CI hardened with `--cov-fail-under=100` and HTML report existence checks for pip-audit and trivy workflows.
- `master` and `main` branch heads synchronized.

## Functional Delivery Status

- E1 Foundation and setup: complete.
- E2 Registry and metadata: complete.
- E3 Router engine: complete.
- E4 CLI: complete.
- E5 Fallback reliability: complete.
- E6 Guardrails: complete.
- E7 Metrics: complete.
- E8 MCP transport/tools: complete.
- E9 CI/CD and security automation: in progress (remaining forced-failure validation task).

## Quality and Validation

- Full test suite passing.
- Coverage currently enforced at 100% in CI test job.
- Security workflows generate and validate HTML artifacts:
  - `security_reports/pip_audit_latest.html`
  - `security_reports/trivy_latest.html`

## Remaining Work to Close E9

1. Execute and document forced-failure CI validation (T8.3).
2. Remove temporary pip-audit CVE ignore (`CVE-2026-3219`) after upstream dependency fix is available.
3. Keep release governance docs aligned with workflow changes.

## Upgrade Notes

Install:

- `pip install efficient-agent-router-ear==0.10.1`

## Verification Checklist

After release publish:

1. Confirm package page appears on PyPI.
2. Confirm wheel and sdist artifacts are present.
3. Confirm install and import smoke test succeeds.
4. Confirm security report HTML artifacts are attached in workflow artifacts.
